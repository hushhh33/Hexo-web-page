# 网站维护注意事项

本文档记录本次维护中遇到的主题配置、编码、文章同步和部署问题，供下次维护时复用。

## 目录职责

- `page1`：测试目录。先在这里修改、预览、构建，确认无误后再同步到正式仓库。
- `page/Hexo-web-page`：正式 Git 仓库。测试通过后，把 `page1` 中的源码和资源同步到这里，再用这个仓库提交、推送。
- 远程仓库：`https://github.com/hushhh33/Hexo-web-page.git`
- 源码分支：`main`
- 静态网页发布分支：`branch`

注意：`page1` 本身不是正式 Git 仓库。之前推送成功的是 `page1/public` 生成后的静态网页到 `branch`，不是 `page1` 源码本身。

## 推荐维护流程

1. 在 `page1` 修改和测试。
2. 使用直接调用 Hexo 的方式构建，避免路径里的 `&` 影响 npm shim：

```powershell
cd "E:\IDE&tools\front page\hexo page\page1"
node .\node_modules\hexo\bin\hexo generate
node .\node_modules\hexo\bin\hexo server
```

3. 浏览器检查：`http://localhost:4000/Hexo-web-page/`
4. 测试通过后，只同步源码和资源到 `page/Hexo-web-page`：
   - `_config.yml`
   - `_config.butterfly.yml`
   - `source/`
   - `scaffolds/`
   - `package.json`
   - `package-lock.json`
   - 需要保留的说明文档
5. 不要同步这些目录：
   - `.git/`
   - `node_modules/`
   - `public/`
   - `.deploy_git/`
6. 在 `page/Hexo-web-page` 里重新构建验证，然后提交推送 `main`。
7. 如果需要更新线上静态网页，再从正式仓库执行 Hexo 部署到 `branch`。

## 本次主题配置变更

主题使用 `hexo-theme-butterfly`。不要直接改 `node_modules/hexo-theme-butterfly`，应使用站点级覆盖文件：

```text
_config.butterfly.yml
```

本次新增/修改的重点：

- 首页标题：`Hushhh的小站`
- 首页背景：`source/images/site-background.jpg`
- 头像：`source/images/avatar.jpg`
- 头像转动关闭：`avatar.effect: false`
- 首页副标题 motto 自动切换保留：`subtitle.effect: true`
- 顶部导航补全：首页、归档、分类、标签、关于
- Announcement：`我要读博啦`
- 页脚透明：通过 `source/css/custom.css`
- About、归档、分类、标签页背景透明：通过 `top_img: false` 和 `custom.css`

## 分类和标签入口页

Butterfly 需要显式入口页，否则只有具体分类/标签路由，缺少 `/categories/` 和 `/tags/` 总入口。

已补：

```text
source/categories/index.md
source/tags/index.md
```

关键 front matter：

```yaml
type: categories
layout: page
top_img: false
```

```yaml
type: tags
layout: page
top_img: false
```

## About 页面编码问题

本次 `source/about/index.md` 出现了全篇乱码，原因是历史内容发生过编码错读/错写。表现包括：

- `鍏充簬鎴?`
- `涓汉绠€浠?`
- YAML 头部被挤成一行：`title: ... date: ...`

处理原则：

- 不要试图在原乱码上继续小修小补。
- 直接重写为 UTF-8 内容。
- 确保 front matter 每个字段独占一行。
- About 页关闭页头大背景：`top_img: false`。
- 添加 `type: about`，方便 CSS 精准控制背景。

当前 About 文件关键头部：

```yaml
---
title: 关于
date: 2025-04-14 15:00:00
layout: page
type: about
top_img: false
avatar: /images/avatar.jpg
---
```

## 自定义 CSS

自定义样式文件：

```text
source/css/custom.css
```

Butterfly 注入配置在 `_config.butterfly.yml`：

```yaml
inject:
  head:
    - <link rel="stylesheet" href="/Hexo-web-page/css/custom.css">
```

注意路径要用 `/Hexo-web-page/css/custom.css`，不要用相对路径 `css/custom.css`。否则归档、分类、标签等子路径页面可能找不到 CSS。

## 文章同步注意

本次从 `page1` 同步到正式仓库时，也带入了文章修改：

- `source/_posts/小程序开发指南.md` 新增了瀑布模型和 scrum 两行笔记。
- `source/_posts/显卡直连问题.md` 是 `page1` 里已有的新文章。

如果下次只想同步主题，不想同步文章，需要在同步前单独比较 `source/_posts/`。

## Git 和部署注意

- `page/Hexo-web-page` 的 `main` 是源码分支。
- `branch` 是 Hexo 静态部署分支。
- `hexo deploy` 会把 `public/` 内容推到 `_config.yml` 中的 `deploy.branch`，当前是 `branch`。
- GitHub 认证不要使用密码。需要 Git Credential Manager、PAT、SSH key 或已登录的凭据。
- 本次曾遇到 GitHub HTTPS 网络断开：`curl 56`、`curl 52`、`Failed to connect to github.com port 443`。这类问题不是代码问题，等网络恢复后重推即可。

当前如果正式仓库显示 `main...origin/main [ahead N]`，表示本地还有提交没推上 GitHub。网络恢复后执行：

```powershell
cd "E:\IDE&tools\front page\hexo page\page\Hexo-web-page"
git push origin main
```

## 删除和覆盖原则

- 不使用批量删除命令。
- 同步时只覆盖需要的源码和资源文件。
- 不删除 `page` 中未知文件。
- 不覆盖 `.git`、`node_modules`、`public`、`.deploy_git`。

## 快速检查命令

```powershell
# 测试目录构建
cd "E:\IDE&tools\front page\hexo page\page1"
node .\node_modules\hexo\bin\hexo generate

# 正式仓库构建
cd "E:\IDE&tools\front page\hexo page\page\Hexo-web-page"
node .\node_modules\hexo\bin\hexo generate

# 正式仓库状态
git status --short --branch

# 查看远程分支
git ls-remote origin refs/heads/main
git ls-remote origin refs/heads/branch
```
